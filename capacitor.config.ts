import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.pdr.shotcrete",
  appName: "Shotcrete Calc Batch",
  webDir: ".output/public",
};

export default config;